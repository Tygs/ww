Add features to strings with s()
=================================

.. automodule:: ww.wrappers.strings

.. autoclass:: ww.wrappers.strings.StringWrapper
   :members:
   :special-members:

Most of those features are just wrappers around pure functions. We exposed them in ":doc:`string_tools`" in case you want to use them directly on regular Python strings.
